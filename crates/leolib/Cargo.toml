[package]
name = "leolib"
description = "Leo's outline model and external-file machinery. No view, ever."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
regex = "1"
quick-xml = "0.37"
once_cell = "1"

[dev-dependencies]
tempfile = "3"
